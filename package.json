{
  "private": true,
  "scripts": {
    "build": "rimraf dist && pnpm -r run build",
    "export": "rimraf dist && pnpm -r run export",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@slidev/cli": "latest",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "@suxiong/eslint-config": "^1.0.0",
    "eslint": "^8.38.0",
    "pnpm": "^8.2.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  }
}
